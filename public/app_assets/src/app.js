define([
    'backbone',
    'marionette',
    'jquery',
    'underscore',
    'json2'
],
function (Backbone, Marionette, $, _) {
    "use strict";

    var app = new Marionette.Application();

    return app;
});
